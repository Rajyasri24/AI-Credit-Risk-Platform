services:
  credit-risk-app:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: credit-risk-platform

    ports:
      - "8501:8501"

    env_file:
      - .env

    volumes:
      - ./data:/app/data
      - ./models:/app/models

    restart: unless-stopped